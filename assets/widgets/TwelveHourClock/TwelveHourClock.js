var bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
  extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
  hasProp = {}.hasOwnProperty,
  modulo = function(a, b) { return (+a % (b = +b) + b) % b; };

Dashing.TwelveHourClock = (function(superClass) {
  extend(TwelveHourClock, superClass);

  function TwelveHourClock() {
    this.startTime = bind(this.startTime, this);
    return TwelveHourClock.__super__.constructor.apply(this, arguments);
  }

  TwelveHourClock.prototype.ready = function() {
    return setInterval(this.startTime, 1000);
  };

  TwelveHourClock.prototype.startTime = function() {
    var hours, meridiem, minutes, today;
    today = new Date();
    hours = this.getHours(today.getHours());
    minutes = this.formatTime(today.getMinutes());
    // meridiem = this.getMeridiem(today.getHours());
    this.set('time', hours + ":" + minutes);
    return this.set('date', today.toDateString());
  };

  TwelveHourClock.prototype.getHours = function(i) {
    return i;
  };

  TwelveHourClock.prototype.formatTime = function(i) {
    if (i < 10) {
      return "0" + i;
    } else {
      return i;
    }
  };

  return TwelveHourClock;

})(Dashing.Widget);

// ---
// generated by coffee-script 1.9.2